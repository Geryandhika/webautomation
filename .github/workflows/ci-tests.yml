name: Web & API Cucumber Tests

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - master

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run API tests
        run: ./gradlew apiTest

      - name: Run Web tests (headless)
        env:
          # bisa gunakan variable tambahan jika ingin headless via arg di driver
          CI: true
        run: ./gradlew webTest

      - name: Upload Cucumber reports
        uses: actions/upload-artifact@v4
        with:
          name: cucumber-reports
          path: |
            app/build/reports/cucumber-api.html
            app/build/reports/cucumber-api.json
            app/build/reports/cucumber-web.html
            app/build/reports/cucumber-web.json
